import { initializeApp } from "firebase/app";
import { getFirestore, collection, getDocs } from 'firebase/firestore/lite';

export const firebaseApp = initializeApp( {

    // Add  config from firebase account here

  
  });

  
  